# Handoff

## Recent Changes
- Created `assets/header.svg` with custom SVG content.
- Updated `README.md` to remove the old `capsule-render` and `readme-typing-svg` external service URLs.
- Replaced the old header images in `README.md` with the newly created local `assets/header.svg` image.
